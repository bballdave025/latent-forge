# Captured branches — not current scope

These are deliberate hooks, not promises to implement them before the current milestone.

- Bayesian probabilistic matrix factorization
- per-user noise / sigma
- per-item noise / sigma
- weighted least squares
- confidence weighting
- temporal drift
- neural collaborative filtering
- side information
- implicit feedback

Before promoting any branch to the trunk, apply the Dave Rule and identify a concrete reason it should reduce holdout MSE.
